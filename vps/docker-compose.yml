services:
  hackvideo:
    build:
      context: ..
      dockerfile: vps/Dockerfile
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./data:/app/vps/data
